# Anthropic API Configuration
# Get your API key from: https://console.anthropic.com/settings/keys
VITE_ANTHROPIC_API_KEY=your_api_key_here

# IMPORTANT SECURITY NOTE:
# This approach exposes your API key in the browser's network tab.
# For production use, you MUST create a backend proxy endpoint.
# 
# Quick backend proxy example (Express):
# app.post('/api/tarot', async (req, res) => {
#   const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });
#   const response = await anthropic.messages.create(req.body);
#   res.json(response);
# });
